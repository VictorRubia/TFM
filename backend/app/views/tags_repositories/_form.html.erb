<%= form_with(model: tags_repository) do |form| %>
  <% if tags_repository.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tags_repository.errors.count, "error") %> prohibited this tags_repository from being saved:</h2>

      <ul>
        <% tags_repository.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :name_wearos, style: "display: block" %>
    <%= form.text_field :name_wearos %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_field :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
